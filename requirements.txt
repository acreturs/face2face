# Python dependencies for face2face project
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.5.0

# Image processing
# NOTE: install ONLY opencv-contrib-python — it is a superset that includes the
# base cv2 AND the contrib modules (cv2.face.createFacemarkLBF, used by
# python/landmarks.py). Installing opencv-python alongside it breaks cv2.face.
opencv-contrib-python>=4.5.0
Pillow>=8.0.0
pillow-heif>=0.10.0

# Data handling
h5py>=3.0.0
PyYAML>=5.4.0

# Biwi dataset downloader (python/get_biwi.py): partial zip download over HTTP
requests>=2.25.0
remotezip>=0.12.0

# Optional: for web viewer
Flask>=2.0.0
